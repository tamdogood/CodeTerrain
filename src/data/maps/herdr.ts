import type { SystemMap } from "@/lib/system-map";

const commit = "51b7064ef0a02642393bab1d2eea0f4dbd8414d2";

const sources = {
  mainDefault: ["src/main.rs", "Default launch and attach", "L789-L812"],
  autoDetect: ["src/server/autodetect.rs", "Server detection and startup", "L280-L306"],
  clientConnect: ["src/client/mod.rs", "Thin-client connection", "L1207-L1370"],
  clientIo: ["src/client/mod.rs", "Host input and terminal output", "L1514-L1709"],
  wireClient: ["src/protocol/wire.rs", "Client protocol messages", "L341-L452"],
  wireFraming: ["src/protocol/wire.rs", "Bounded binary framing", "L900-L966"],
  clientTransport: ["src/server/client_transport.rs", "Client socket transport", "L515-L690"],
  apiConnection: ["src/api/server.rs", "JSON API connection", "L161-L309"],
  apiDispatch: ["src/api/server.rs", "API request dispatch", "L817-L855"],
  apiSchema: ["src/api/schema.rs", "API request schema", "L33-L95"],
  apiEvents: ["src/api/event_hub.rs", "Bounded API event ring", "L1-L45"],
  headlessLoop: ["src/server/headless.rs", "Headless server event loop", "L535-L817"],
  headlessApi: ["src/server/headless.rs", "API method handling", "L3520-L3800"],
  appRuntime: ["src/app/mod.rs", "Application runtime registry", "L100-L165"],
  appState: ["src/app/state.rs", "Workspace state model", "L1366-L1395"],
  uiCompute: ["src/ui.rs", "View geometry computation", "L108-L156"],
  renderStream: ["src/server/render_stream.rs", "Per-client render stream", "L1-L112"],
  paneRuntime: ["src/pane.rs", "Pane terminal runtime", "L2020-L2155"],
  paneSpawn: ["src/pane.rs", "Shell process spawning", "L1698-L1857"],
  ptyActor: ["src/pty/actor/unix.rs", "PTY actor command loop", "L351-L455"],
  paneTerminal: ["src/pane/terminal.rs", "Terminal mutation handling", "L190-L219"],
  detector: ["src/pane.rs", "Coding-agent detection", "L2157-L2540"],
  snapshot: ["src/persist/snapshot.rs", "Session snapshot capture", "L251-L307"],
  restore: ["src/persist/restore.rs", "Session restoration", "L257-L303"],
  remoteAttach: ["src/remote/attach.rs", "Remote attach setup", "L162-L199"],
  remoteBridge: ["src/remote/attach.rs", "SSH stdio bridge", "L1661-L1738"],
  remoteHost: ["src/remote/host_unix.rs", "Remote socket byte pumps", "L8-L68"],
  manifestFeed: ["src/detect/manifest_update.rs", "Detection manifest feed", "L202-L250"],
  updateFeed: ["src/update.rs", "Release feed and update check", "L322-L350"],
} as const;

function cite(source: keyof typeof sources) {
  const [path, label, lines] = sources[source];
  return [{ label, path, url: `https://github.com/herdrdev/herdr/blob/${commit}/${path}#${lines}` }];
}

export const herdrMap = {
  slug: "herdr",
  title: "Herdr terminal workspace runtime",
  subtitle:
    "From host input through private sockets, persistent workspace state, pane-owned PTYs, agent detection, and per-client rendering.",
  orientation:
    "Start with the interactive loop, then compare the JSON automation path with remote attach and session restoration.",
  snapshot: { branch: "master", commit, analyzedAt: "2026-08-16" },
  nodes: [
    {
      id: "launcher",
      label: "Launcher & host TTY",
      district: "Clients",
      kind: "entry",
      summary: "Chooses CLI, remote, daemon, thin-client, or monolithic mode.",
      responsibility: "Route arguments, select a session, detect the server, and own host stdin and stdout.",
      x: 10, y: 40, height: 2,
      citations: cite("mainDefault"),
    },
    {
      id: "client",
      label: "Thin TUI Client",
      district: "Clients",
      kind: "entry",
      summary: "Bridges the host terminal to Herdr's private binary protocol.",
      responsibility: "Manage raw mode, input and resize readers, protocol handshakes, and local render output.",
      x: 26, y: 40, height: 3,
      citations: cite("clientConnect"),
    },
    {
      id: "cli",
      label: "CLI & Agent Clients",
      district: "Automation",
      kind: "tooling",
      summary: "Controls workspaces, panes, and agent state through the JSON API.",
      responsibility: "Send newline-delimited requests and consume ordinary, waiting, or streaming responses.",
      x: 10, y: 72, height: 2,
      citations: cite("apiSchema"),
    },
    {
      id: "wire",
      label: "Binary Gateway",
      district: "Transport",
      kind: "service",
      summary: "Carries bounded, versioned messages between thin clients and the server.",
      responsibility: "Own the client socket, handshake, protocol checks, frame bounds, and connection writers.",
      x: 42, y: 40, height: 3,
      citations: cite("wireFraming"),
    },
    {
      id: "api",
      label: "JSON API Gateway",
      district: "Transport",
      kind: "service",
      summary: "Accepts one NDJSON request or a long-lived event stream per connection.",
      responsibility: "Parse requests, dispatch them to the app, and serialize responses or event envelopes.",
      x: 42, y: 72, height: 3,
      citations: cite("apiConnection"),
    },
    {
      id: "server",
      label: "Headless Server",
      district: "Control plane",
      kind: "service",
      summary: "Keeps sessions and terminal processes alive after every TUI disconnects.",
      responsibility: "Own the main event loop, App lifetime, sockets, clients, scheduling, and shutdown.",
      x: 58, y: 40, height: 4,
      citations: cite("headlessLoop"),
    },
    {
      id: "state",
      label: "AppState & EventHub",
      district: "Control plane",
      kind: "compute",
      summary: "Models workspaces, focus, panes, runtimes, and recent API events.",
      responsibility: "Serialize state changes and route terminal, API, detection, and render work.",
      x: 74, y: 40, height: 3,
      citations: cite("appState"),
    },
    {
      id: "renderer",
      label: "Virtual Renderer",
      district: "Rendering",
      kind: "compute",
      summary: "Produces semantic frames or ANSI diffs from in-memory terminal state.",
      responsibility: "Compute layout, render a virtual buffer, and keep a baseline for each connected client.",
      x: 90, y: 24, height: 3,
      citations: cite("renderStream"),
    },
    {
      id: "pty",
      label: "Terminal Runtime & PTYs",
      district: "Runtime",
      kind: "runtime",
      summary: "Owns each pane's child process, terminal state, and PTY I/O actor.",
      responsibility: "Queue user writes, resize and lifecycle controls, PTY reads, and child-process events.",
      x: 90, y: 55, height: 4,
      citations: cite("paneRuntime"),
    },
    {
      id: "vt",
      label: "Ghostty VT & Detector",
      district: "Runtime",
      kind: "runtime",
      summary: "Parses terminal bytes and detects coding-agent state from output and process identity.",
      responsibility: "Maintain cells and terminal modes, match manifests, and emit render and agent-state events.",
      x: 74, y: 72, height: 3,
      citations: cite("detector"),
    },
    {
      id: "agent",
      label: "Shell & Agent Process",
      district: "Processes",
      kind: "external",
      summary: "Runs behind a portable PTY as an ordinary observed child process.",
      responsibility: "Execute commands and exchange input, output, resize, signal, and lifecycle events.",
      x: 90, y: 86, height: 3,
      citations: cite("paneSpawn"),
    },
    {
      id: "store",
      label: "Session Storage",
      district: "Persistence",
      kind: "storage",
      summary: "Atomically saves model structure and cached detection manifests.",
      responsibility: "Persist workspace metadata and restore fresh shells at saved working directories.",
      x: 58, y: 86, height: 2,
      citations: cite("snapshot"),
    },
    {
      id: "remote",
      label: "SSH Stdio Bridge",
      district: "Remote",
      kind: "external",
      summary: "Proxies the normal binary client protocol over SSH without a second protocol.",
      responsibility: "Own a local forwarding socket, SSH child, remote bridge command, and byte pumps.",
      x: 42, y: 10, height: 2,
      citations: cite("remoteBridge"),
    },
    {
      id: "distribution",
      label: "herdr.dev Feeds",
      district: "Distribution",
      kind: "external",
      summary: "Publishes release metadata, binaries, checksums, and detection manifests.",
      responsibility: "Supply verified executable updates and bounded TOML detector rules.",
      x: 90, y: 8, height: 2,
      citations: cite("updateFeed"),
    },
  ],
  edges: [
    {
      id: "launch-daemon", from: "launcher", to: "server", kind: "control", label: "spawn daemon",
      payload: "server subcommand → detached process → socket readiness",
      description: "The default command starts the headless server only when the client socket is absent.",
      citations: cite("mainDefault"),
    },
    {
      id: "launch-client", from: "launcher", to: "client", kind: "control", label: "attach client",
      payload: "client mode + host TTY + socket path",
      description: "After readiness, the launcher enters thin-client mode against a local or forwarded socket.",
      citations: cite("autoDetect"),
    },
    {
      id: "stdin-client", from: "launcher", to: "client", kind: "control", label: "stdin & resize",
      payload: "RawInputEvent + terminal dimensions",
      description: "Reader threads forward keyboard, mouse, paste, byte, and resize events.",
      citations: cite("clientIo"),
    },
    {
      id: "client-wire", from: "client", to: "wire", kind: "control", label: "ClientMessage",
      payload: "Hello | Input | Resize | Detach",
      description: "The thin client writes typed, versioned messages through the private binary socket.",
      citations: cite("wireClient"),
    },
    {
      id: "wire-server", from: "wire", to: "server", kind: "control", label: "validated event",
      payload: "ClientConnected | ClientInput | ClientResize | …",
      description: "The gateway validates frames and converts them into server-owned events.",
      citations: cite("clientTransport"),
    },
    {
      id: "server-state", from: "server", to: "state", kind: "control", label: "dispatch",
      payload: "input event | app event | API method",
      description: "The event loop serializes client and API work through the App.",
      citations: cite("headlessLoop"),
    },
    {
      id: "state-pty", from: "state", to: "pty", kind: "control", label: "terminal command",
      payload: "bytes + resize, spawn, focus, or lifecycle command",
      description: "Input routing resolves the focused pane and sends work to its PTY actor.",
      citations: cite("appRuntime"),
    },
    {
      id: "pty-agent", from: "pty", to: "agent", kind: "data", label: "PTY input",
      payload: "user input bytes + window size",
      description: "The actor drains queued writes and applies control messages to the child pseudo-terminal.",
      citations: cite("ptyActor"),
    },
    {
      id: "agent-vt", from: "agent", to: "vt", kind: "data", label: "PTY output",
      payload: "stdout/stderr bytes + foreground process identity",
      description: "PTY reads feed the parser while detection separately probes the foreground process.",
      citations: cite("paneRuntime"),
    },
    {
      id: "vt-state", from: "vt", to: "state", kind: "data", label: "terminal mutation",
      payload: "cells, modes, title, CWD, AgentState, RenderSignal",
      description: "Parsed effects and detected agent state become serialized app events.",
      citations: cite("paneTerminal"),
    },
    {
      id: "state-render", from: "state", to: "renderer", kind: "data", label: "render model",
      payload: "AppState + terminal snapshots + client area and theme",
      description: "Geometry and terminal state are read into a virtual render buffer.",
      citations: cite("uiCompute"),
    },
    {
      id: "render-server", from: "renderer", to: "server", kind: "data", label: "frame diff",
      payload: "FrameData | TerminalFrame",
      description: "Per-client baselines suppress identical frames and produce compact ANSI diffs.",
      citations: cite("renderStream"),
    },
    {
      id: "server-wire", from: "server", to: "wire", kind: "data", label: "ServerMessage",
      payload: "Welcome | Frame | Terminal | controls",
      description: "The server enqueues a prepared frame for the connection writer.",
      citations: cite("headlessLoop"),
    },
    {
      id: "wire-client", from: "wire", to: "client", kind: "data", label: "framed bytes",
      payload: "[u32 length][bincode ServerMessage]",
      description: "The client reassembles bounded frames and returns typed messages to the terminal loop.",
      citations: cite("wireFraming"),
    },
    {
      id: "client-stdout", from: "client", to: "launcher", kind: "data", label: "screen output",
      payload: "ANSI bytes or locally painted FrameData",
      description: "The client writes terminal diffs or semantic frames back to the host display.",
      citations: cite("clientIo"),
    },
    {
      id: "cli-api", from: "cli", to: "api", kind: "control", label: "NDJSON request",
      payload: "{ id, method, params } + newline",
      description: "A CLI or coding agent opens the private API socket and sends one tagged request.",
      citations: cite("apiSchema"),
    },
    {
      id: "api-server", from: "api", to: "server", kind: "control", label: "API request",
      payload: "request + response channel + stream state",
      description: "The connection thread sends parsed work into the headless event loop.",
      citations: cite("apiDispatch"),
    },
    {
      id: "state-server-response", from: "state", to: "server", kind: "data", label: "API result",
      payload: "success | error | event envelope",
      description: "The App handles the method, publishes state changes, and completes the response channel.",
      citations: cite("headlessApi"),
    },
    {
      id: "server-api-response", from: "server", to: "api", kind: "data", label: "response channel",
      payload: "serialized result or stream cursor",
      description: "The waiting API connection receives a result or continues polling the event ring.",
      citations: cite("apiDispatch"),
    },
    {
      id: "state-events", from: "state", to: "api", kind: "data", label: "event ring",
      payload: "EventEnvelope + monotonic sequence",
      description: "Agent state changes are retained for waiting or streaming clients.",
      citations: cite("apiEvents"),
    },
    {
      id: "api-cli-response", from: "api", to: "cli", kind: "data", label: "JSON result",
      payload: "success | error | event envelope",
      description: "The gateway returns one JSON line or a sequence of subscription events.",
      citations: cite("apiConnection"),
    },
    {
      id: "state-store", from: "state", to: "store", kind: "state", label: "atomic save",
      payload: "SessionSnapshot JSON → temp file → rename",
      description: "A debounced capture atomically replaces the saved session model.",
      citations: cite("snapshot"),
    },
    {
      id: "store-state", from: "store", to: "state", kind: "state", label: "restore",
      payload: "session JSON → AppState + fresh terminals",
      description: "Startup rebuilds saved structure and starts a fresh shell in each stored working directory.",
      citations: cite("restore"),
    },
    {
      id: "client-ssh", from: "client", to: "remote", kind: "data", label: "local forward",
      payload: "binary client protocol through a private socket",
      description: "Remote launch points the normal thin client at a local socket owned by the SSH bridge.",
      citations: cite("remoteAttach"),
    },
    {
      id: "ssh-wire", from: "remote", to: "wire", kind: "data", label: "SSH upload",
      payload: "opaque client bytes → SSH stdin → remote socket",
      description: "The remote helper copies SSH input into the normal remote client socket.",
      citations: cite("remoteBridge"),
    },
    {
      id: "wire-ssh", from: "wire", to: "remote", kind: "data", label: "SSH download",
      payload: "opaque ServerMessage bytes → SSH stdout",
      description: "Binary socket output returns over SSH without another application protocol.",
      citations: cite("remoteHost"),
    },
    {
      id: "ssh-client", from: "remote", to: "client", kind: "data", label: "forwarded frame",
      payload: "length-prefixed ServerMessage through local socket",
      description: "The local bridge returns SSH output bytes to the ordinary thin client.",
      citations: cite("remoteBridge"),
    },
    {
      id: "release-launcher", from: "distribution", to: "launcher", kind: "state", label: "release feed",
      payload: "release JSON → asset URL + SHA-256 → executable",
      description: "Manual update downloads the platform asset, verifies its checksum, and replaces the executable.",
      citations: cite("updateFeed"),
    },
    {
      id: "manifest-vt", from: "distribution", to: "vt", kind: "state", label: "detector TOML",
      payload: "manifest index + bounded per-agent TOML",
      description: "The updater validates, stores, and reloads remotely published matching rules.",
      citations: cite("manifestFeed"),
    },
    {
      id: "release-remote", from: "distribution", to: "remote", kind: "state", label: "remote binary",
      payload: "platform asset + protocol metadata + checksum",
      description: "Remote preparation can install a compatible binary before opening the bridge.",
      citations: cite("remoteAttach"),
    },
  ],
  journeys: [
    {
      id: "interactive", label: "Keystroke → agent → screen",
      summary: "Cross the binary socket, focused PTY, terminal parser, server renderer, and host display.",
      edgeIds: ["stdin-client", "client-wire", "wire-server", "server-state", "state-pty", "pty-agent", "agent-vt", "vt-state", "state-render", "render-server", "server-wire", "wire-client", "client-stdout"],
    },
    {
      id: "startup", label: "Default launch & attach",
      summary: "Probe the private socket, start or restore the server, then attach a thin client.",
      edgeIds: ["launch-daemon", "store-state", "launch-client"],
    },
    {
      id: "automation", label: "Coding-agent API call",
      summary: "Send NDJSON through the API socket, serialize work in AppState, and return a response.",
      edgeIds: ["cli-api", "api-server", "server-state", "state-pty", "pty-agent", "state-server-response", "server-api-response", "api-cli-response"],
    },
    {
      id: "detection", label: "Agent-state detection",
      summary: "Observe PTY output and process identity, update AppState, and notify API subscribers.",
      edgeIds: ["agent-vt", "manifest-vt", "vt-state", "state-events", "api-cli-response"],
    },
    {
      id: "persistence", label: "Save, restart & restore",
      summary: "Persist model structure atomically and recreate fresh terminal processes after restart.",
      edgeIds: ["state-store", "store-state", "state-pty", "pty-agent"],
    },
    {
      id: "remote", label: "Local TUI → remote server",
      summary: "Reuse the binary client protocol through a local socket and SSH stdio bridge.",
      edgeIds: ["release-remote", "launch-client", "client-ssh", "ssh-wire", "wire-server", "server-wire", "wire-ssh", "ssh-client"],
    },
    {
      id: "updates", label: "Release & detector feeds",
      summary: "Fetch verified executable releases separately from bounded agent-detection manifests.",
      edgeIds: ["release-launcher", "release-remote", "manifest-vt"],
    },
  ],
  glossary: [
    { term: "PTY", definition: "A pseudo-terminal that lets Herdr host an interactive shell or coding agent as a child process." },
    { term: "Thin client", definition: "A terminal-facing process that owns host I/O but leaves workspaces and PTYs in the server." },
    { term: "AppState", definition: "The in-memory model of workspaces, tabs, panes, focus, modes, and their runtime references." },
    { term: "NDJSON", definition: "Newline-delimited JSON, used so each API request or response occupies one stream line." },
    { term: "Frame baseline", definition: "The previous screen retained per client so unchanged cells do not need to be sent again." },
    { term: "Agent detection", definition: "Inferring a coding agent's state from terminal output, OSC hints, and foreground process identity." },
    { term: "Atomic replace", definition: "Writing a temporary file and renaming it over the destination to avoid partial session data." },
  ],
  learningPath: [
    { title: "Trace one keystroke", description: "Follow host input through ClientMessage, AppState, the pane PTY, Ghostty, and the per-client render stream." },
    { title: "Compare both sockets", description: "Contrast the framed binary TUI protocol with the newline-delimited JSON automation API." },
    { title: "Separate model from process", description: "Read snapshot and restore together to see why layouts persist while shells and PTYs restart fresh." },
  ],
} satisfies SystemMap;
